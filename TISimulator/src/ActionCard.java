import java.util.HashMap;

public class ActionCard {
	
	ActionCard(){
		
	}
	
	//comb over all action cards, note what they should get passed in and what they should return
	//make a json file of all the values that they'll implement
	
	// OOORRRR make a class file for each card (slower but might be the only way...)
	
	void useAction(HashMap<String, int[]> gameState){
		//check if timing is valid
		
		// for gameStateKey in gameState
			// gameStateValue = actionCard[gameStateKey]
	}
}
